# ModelDock does not read provider keys from server environment variables.
# Enter keys in Providers. They remain in memory unless you enable the local vault.
# Keep all real credentials out of source control.
# Optional local development settings (normally no .env file is needed):
# SITES_BUILD_TIMEOUT=3m
